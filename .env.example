# Django Configuration
SECRET_KEY=django-insecure-prod-inventory-stock-secret-key-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,web

# Database Configuration
# Leave DATABASE_URL empty or unset to use SQLite for local development.
# For PostgreSQL (e.g. Docker or local Postgres):
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/inventory_db
# Or use Docker compose service name:
# DATABASE_URL=postgres://postgres:postgres@db:5432/inventory_db
DATABASE_URL=
USE_POSTGRES=0
